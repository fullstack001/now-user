import '/components/buttons-management/part_of_file/part.dart';

class FarenowButtonStyleModel {
  Color? buttonColor;
  Color? textColor;

  EdgeInsetsGeometry? padding;
  double? height;
  FarenowButtonStyleModel(
      {this.buttonColor, this.textColor, this.padding, this.height});
}
